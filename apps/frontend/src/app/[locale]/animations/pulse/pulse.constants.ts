/**
 * @file Pulse Effect Constants
 * 
 * Default configurations for the PulseEffect component.
 */

// Default configuration values
export const DEFAULT_RING_COLOR = 'rgba(255, 255, 255, 1)';
export const DEFAULT_MAX_RADIUS = 100; // pixels
export const DEFAULT_DURATION = 1000; // milliseconds

// Edge softness defaults
export const DEFAULT_OUTER_BLUR = 20; // pixels
export const DEFAULT_OUTER_SPREAD = 10; // pixels
export const DEFAULT_INNER_BLUR = 20; // pixels
export const DEFAULT_INNER_SPREAD = 5; // pixels

// Opacity defaults
export const DEFAULT_INITIAL_OPACITY = 1;
export const DEFAULT_ANIMATION_OPACITY = 1;
export const DEFAULT_FINAL_OPACITY = 0;

// Fade timing defaults
export const DEFAULT_FADE_IN_DURATION = 0; // milliseconds
export const DEFAULT_FADE_IN_TO_ANIMATION_DURATION = 0; // milliseconds
export const DEFAULT_FADE_OUT_DURATION = 200; // milliseconds

// Animation direction and easing
export const DEFAULT_DIRECTION: 'expand' | 'shrink' = 'expand';
export const DEFAULT_EASING: 'linear' | 'ease-in-out' | 'ease-out' | 'ease-in' = 'linear';

